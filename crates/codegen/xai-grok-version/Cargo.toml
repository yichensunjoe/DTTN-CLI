[package]
license = "Apache-2.0"
name = "xai-grok-version"
version = "0.2.0-dev"
edition.workspace = true
build = "build.rs"
description = "Lockstepped DTTN CLI version sourced from the shipping binary package."

[dependencies]
semver = { workspace = true }

[lints]
workspace = true
